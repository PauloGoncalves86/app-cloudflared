---
name: Lock

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: "25 3 * * *"
  workflow_dispatch:

permissions:
  issues: write
  contents: read

jobs:
  workflows:
    uses: hassio-addons/workflows/.github/workflows/lock.yaml@main
